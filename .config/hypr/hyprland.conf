# See https://wiki.hyprland.org/Configuring/Monitors/
# List monitors with 'hyprctl monitors'
monitor=eDP-1, 1920x1080@144, 0x0, 1
monitor=HDMI-A-1, 1920x1080, 1920x0, 1
# monitor=,highrr,auto,1

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
exec-once = hyprctl setcursor Bibata-Modern-TokyoNight 24
exec-once = waybar
exec-once = hyprpaper
exec-once = dunst
exec-once=/usr/lib/polkit-kde-authentication-agent-1

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# Some default env vars.
env = XCURSOR_SIZE,24

# Session
env,= XDG_S,SSION_TYPE,wayland
env = XDG_S,SSION_DESKTOP,Hyprland
env = XDG_CURRENT_DESKTOP,Hyprland

# Wayland stuff
env = MOZ_ENABLE_WAYLAND,1
env = SDL_VIDEODRIVER,wayland
env = _JAVA_AWT_WM_NONREPARENTING,1
env = CLUTTER_BACKEND,wayland

# Waland Qt stuff
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = QT_QPA_PLATFORMTHEME,qt5ct
env = QT_QPA_PLATFORM,wayland
env = QT_WAYLAND_DISABLE_WINDOWDECORATIONS,1

# Nvidia specific
env = LIBVA_DRIVER_NAME,nvidia
env = XDG_SESSION_TYPE,wayland
# env = GBM_BACKEND,nvidia-drm      # crashes for me
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = WLR_NO_HARDWARE_CURSORS,1

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
# Also see /usr/share/X11/xkb/rules/base.lst
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options = shift:both_shiftlock,ctrl:nocaps #,altwin:swap_lalt_lwin
    kb_rules =

    follow_mouse = 1

    touchpad {
        disable_while_typing = true
        natural_scroll = no
        drag_lock = true
    }

    sensitivity = -0.1 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    gaps_in = 5
    gaps_out = 10
    border_size = 2
    no_border_on_floating = true
    col.active_border = rgba(5e548eff) rgba(e0b1cbff) 135deg
    col.inactive_border = rgba(595959aa)
    cursor_inactive_timeout = 2

    layout = dwindle
    no_focus_fallback = true
    resize_on_border = true
    extend_border_grab_area = 5
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    rounding = 5
    multisample_edges = true
    active_opacity = 1.0
    inactive_opacity = 1.0
    fullscreen_opacity = 1.0

    drop_shadow = yes
    shadow_range = 6
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)
    # screen_shader = path/to/shader.frag
    blur {
        enabled = true
        size = 8
        passes = 1
        noise = 0.23
    }
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = yes # you probably want this
}

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = on
}

misc {
    disable_hyprland_logo = true
    mouse_move_enables_dpms = true
    key_press_enables_dpms = true
    # animate_manual_resizes = true
    focus_on_activate = true
}


# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic-mouse-v1 {
    sensitivity = -0.5
}

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
#
windowrule = float, ^(dolphin)$
windowrule = float, ^(meld)$
windowrule = float, ^(QGain_gui)$
windowrule = float, ^(Nextcloud)$
# windowrulev2=nofocus,class:^(com.bitwig.BitwigStudio)
windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0
# windowrulev2=stayfocused,class:^(com.bitwig.BitwigStudio)

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = alt

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
# use 'wev' to determine keys
bind = $mainMod, Return, exec, kitty
bind = alt_SHIFT, W, killactive,
bind = alt_SHIFT, P, exec, hyprpicker,
bind = alt_SHIFT, S, exec, hyprshot -m region --clipboard-only,
# bind = $mainMod, M, exit,
bind = $mainMod, E, exec, dolphin
bind = $mainMod, I, exec, firefox
bind = $mainMod, space, exec, wofi --show drun
bind = $mainMod, ;, exec, dunstctl close-all

bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, R, togglesplit, # dwindle
bind = $mainMod, F, togglefloating,
bind = $mainMod, M, fullscreen,
# bind = $mainMod, G, togglegroup,

# Move focus with mainMod + arrow keys
bind = $mainMod, H, movefocus, l
bind = $mainMod, L, movefocus, r
bind = $mainMod, K, movefocus, u
bind = $mainMod, J, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# sets repeatable binds for resizing the active window
binde=alt_SHIFT,L,resizeactive,20 0
binde=alt_SHIFT,H,resizeactive,-20 0
binde=alt_SHIFT,K,resizeactive,0 -20
binde=alt_SHIFT,J,resizeactive,0 20

