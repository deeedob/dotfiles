#Export environment variables | gets calles from .xinitrc
export PATH="$PATH:$HOME/.local/bin:$HOME/bin" #export user specific path
export USER_TERMINAL="alacritty" 
export USER_FILEMANAGER="ranger"
export QT_QPA_PLATFORMTHEME=gtk3 #fix weird QT behaviour on System UX Shemes
export npm_config_prefix="$HOME/.local"
export _JAVA_AWT_WM_NONREPARENTING=1 # fix weird bahaviour with Java UI's (clion)

if [ $mycomputer = "ddobs_desktop" ] ; then
  #export GTK_SCALE=2
fi

